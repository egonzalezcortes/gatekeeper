{
  "name": "gatekeeper-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"require('http').createServer((_, res) => { res.writeHead(200, {'Content-Type': 'text/plain'}); res.end('Frontend placeholder. Vite scaffold comes in Step 11.'); }).listen(5173, '0.0.0.0', () => console.log('Frontend placeholder listening on port 5173'));\""
  },
  "dependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest"
  }
}
